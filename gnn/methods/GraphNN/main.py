import sys
sys.path.append('../../')  # gnn
import time
import argparse
import torch
import torch.nn.functional as F
import numpy as np
import dgl

def run(args):
    # Load data
    
    # Set device
    
    # Set model
    ## GCN
    ## GAT
    ## HAN
    ## GTN
    
    # Set loss and optimizer
    
    # Train model
    for epoch in range(args.epoch):
        # ==================forward==================
        
        # ==================backward=================
        
        # ====================log====================
    
    # Test model